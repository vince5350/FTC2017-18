package org.firstinspires.ftc.teamcode.subsystems;

import org.firstinspires.ftc.teamcode.altDriverPeriod;

public class TelemetryLoop extends altDriverPeriod {
    /*
    private hwMap robot = new hwMap();
    private altDriverPeriod main = new altDriverPeriod();
    public void loop(){
        telemetry.addLine("Motor #1 | ")
                .addData("Speed", robot.FLwheel.getPower());
        telemetry.addLine("Motor #2 | ")
                .addData("Speed", -robot.FRwheel.getPower());
        telemetry.addLine("Motor #3 | ")
                .addData("Speed", robot.BLwheel.getPower());
        telemetry.addLine("Motor #4 | ")
                .addData("Speed", -robot.BRwheel.getPower());
        if(main.gamepad1.b){
            telemetry.addLine("Boost On");
        }

    } */
}
